name: demo
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  #ioc
  provider: ^6.0.0

  #localization
  flutter_sheet_localization: ^5.1.0
  flutter_localizations:
    sdk: flutter


  intl: ^0.17.0

  #network
  dio: ^4.0.1

  #state management
  flutter_bloc: ^7.0.0

  #utils
  freezed_annotation: ^0.14.1

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  freezed: ^0.14.1+1
  build_runner: ^1.12.1

  flutter_sheet_localization_generator: ^5.1.0

  flutter_lints: ^1.0.4

flutter:

  uses-material-design: true
